# 项目更改记录

## 2026-04-14（GitHub：独立仓库并推送到 Zhiyuan-Profile）

### 成功

- 在 `个人网页` 目录内 **`git init`**，与主目录 `/Users/zhiyuansong` 的大仓库解耦，仅跟踪本站文件。
- 新增 **`.gitignore`**（忽略 `.DS_Store`、`.cursor/`）。
- 初始提交后 **`git push -u origin main`** 到远程：[`https://github.com/songzhiyuan98/Zhiyuan-Profile.git`](https://github.com/songzhiyuan98/Zhiyuan-Profile.git)（空仓库已填充）。

### 失败

- 无。

## 2026-04-10（Investment Skill v2：正文改为完整规范稿）

### 成功

- `projects/investment-skill-architecture-v2.html`：由“结构化浓缩版”改为按用户提供文档的**完整展开版**（1–17 节完整条目、允许/禁止/停手/降级/升级、路由等级、角色职责、状态模型、输出契约、MVP 清单与开发原则），保留现有站点版式与版本切换按钮。

### 失败

- 无。

## 2026-04-10（Investment Skill：默认入口切到 v2.0）

### 成功

- `index.html`、`en/index.html`：Investment Skill 首页卡片链接改为默认跳转 **v2.0**（`investment-skill-architecture-v2.html`）。
- `projects/investment-skill-architecture.html`、`en/projects/investment-skill-architecture.html`：版本切换按钮顺序调整为 **v2.0 在前**、v1.0 在后，突出最新版本。
- `projects/investment-skill-architecture-v2.html`、`en/projects/investment-skill-architecture-v2.html`：保持 v2.0 按钮高亮并置于前位。

### 失败

- 无。

## 2026-04-10（Investment Skill：保留 v1 + 新增 v2 切换）

### 成功

- **保留原文件**：`projects/investment-skill-architecture.html` 作为 **架构 v1.0**，正文不删除。
- **新增中文 v2**：`projects/investment-skill-architecture-v2.html`，采用同一套文档壳层（顶部导航、左侧 TOC、`doc-section`、`article-toc.js`），内容按你提供的《投资 Skill 完整系统设计文档》重排为 17 节。
- **同页版本切换按钮**：在 v1 与 v2 页面都加入 `doc-version-switch`，可互跳 `v1.0 / v2.0`。
- **英文镜像同步**：`en/projects/investment-skill-architecture-v2.html` 新增，且 `en/projects/investment-skill-architecture.html` 同步加入版本切换。
- **样式支持**：`css/style.css` 新增 `.doc-version-switch`、`.doc-version-switch__btn`（含 `is-active`）样式。

### 失败

- 无。

## 2026-04-10（Investment Skill：版式对齐站内设计文档）

### 成功

- **`projects/investment-skill-architecture.html`**、**`en/projects/investment-skill-architecture.html`**：页眉改为 **概述（#overview）** + 短导语；**「一 / 1」** 起独立 `doc-section`；**三层 / 四子层 / 最终三层面** 使用既有 **`stat-strip`**；请求类型示例用 **`blockquote`**；Request Router 用 **`doc-columns--2`**；运行模式与 Agent 调用、Full Decision Card 字段用 **`table`**；Full 链用 **`pre.doc-pre`**；协作七步用 **`ol.doc-steps`**；十六节落地清单双栏。目录增加 **概述 / Overview** 锚点。
- **`css/style.css`**：新增 **`.doc-columns` / `.doc-columns--2`**、**`.doc-steps`**（圆角序号）、**`.article-body blockquote ul`** 与 **`article-body ol.doc-steps`** 覆盖默认 `ol` 缩进，避免与设计文档列表冲突。

### 失败

- 无。

## 2026-04-10（Investment Skill：架构页全文对齐用户规范稿）

### 成功

- **`projects/investment-skill-architecture.html`**：按用户提供的《投资多 Agent Skill 完整架构文档》**逐节补全**（一至十六节与最终总结）：含全部列表、示例问句、各 Agent 职责/输入/输出、Quick/Focused/Full 触发与调用关系、协作七步、三种输出字段、Focus 迭代与用户增强、初版落地清单等；页眉 meta/title 去掉 MVP 字样，改为「完整架构文档」。
- **`en/projects/investment-skill-architecture.html`**：与中文**相同锚点 `id`**，英文全文与上述结构一一对照扩展。

### 失败

- 无。

## 2026-04-10（首页：Investment Skill 卡片文案）

### 成功

- `index.html`、`en/index.html`：Investment Skill 卡片 **kicker** 去掉 MVP 版本字样，改为突出 **组合与纪律 / portfolio discipline**；**简介**改为通俗说明「个人账户决策类 Skill、多角色分工、状态与资金约束、减情绪化」，技术细节留给内页。

### 失败

- 无。

## 2026-04-10（Investment Skill：MVP 架构文档 v0.1）

### 成功

- **`projects/investment-skill-architecture.html`**：按《投资多 Agent Skill 完整架构文档》重写为 **MVP v0.1** 正文（目标、总原则、A/B/C 三层、用户交互与 Request Router、Quick/Focused/Full、四子层团队、Chief / 市场风险新闻 / 投研 / 执行 / 状态、调用图、协作顺序、三种输出、Focus 迭代与首页卡片对齐）；目录锚点与 **`article-toc.js`** 一致。
- **`en/projects/investment-skill-architecture.html`**：英文镜像，**与中文版相同 `section`/`header` 的 `id`**，便于语言切换对照。
- **`index.html`**、**`en/index.html`**：Investment Skill 卡片 kicker 由「设想中 / Draft」改为 **MVP v0.1**，描述改为概括三层架构、三种模式、状态与 T+1、角色与调用链；仍注明非投资建议。

### 失败

- 无。

## 2026-04-10

### 成功

- 在项目根目录执行 `npx getdesign@latest add uber`，从 `getdesign` 模板生成 Uber 风格设计说明。
- CLI 将文件写入用户主目录：`/Users/zhiyuansong/DESIGN.md`（getdesign 默认行为）。
- 已将同一份内容同步到本项目：`DESIGN.md`，便于后续 UI 开发直接引用。

### 说明

- 首次在沙箱中运行失败：工具需写入主目录 `DESIGN.md`，沙箱无权限；使用完整权限后执行成功。

## 2026-04-10（下午）

### 成功

- 按 `DESIGN.md` 实现静态前端：`css/style.css`（色板、胶囊导航、卡片阴影、页脚黑底）、`index.html`（首页与占位项目卡片）、`js/main.js`（移动端导航）。
- 真实内容页二：`projects/research-mribag.html`（Nature Medicine 论文元数据、英文摘要原文、要点与数据门户链接）；`projects/tsla-options-war-room.html`（Skill 名称、触发语、体验原则与规则摘要）。

### 失败

- 无。

## 2026-04-10（晚间）

### 成功

- 运行 `npx getdesign@latest add spacex`；因根目录已有 `DESIGN.md`，CLI 将 SpaceX 模板保存至 `~/spacex/DESIGN.md`，并已 `cp` 到项目根目录覆盖为当前生效的 `DESIGN.md`。
- 按 SpaceX 策展规范重做 UI：`css/style.css`（光谱白 `#f0f0fa`、全屏场景、暗叠层、幽灵按钮 32px 圆角、无阴影无卡片、D-DIN 字距）；`index.html` 三幕全幅 Unsplash 影像 + 堆叠文字与链接。
- 子页 `projects/research-mribag.html`、`projects/tsla-options-war-room.html` 改为黑底顶栏 + `article-shell` 阅读区；字体改为 CDN 引入 D-DIN。
- `js/main.js` 注释更新（导航容器类名变更）。

### 说明

- SpaceX 主题为第三方「类官网」策展起点，非官方设计系统；外链大图依赖 Unsplash，离线环境可能不显示背景图。

## 2026-04-10（Figma 主题）

### 成功

- 运行 `npx getdesign@latest add figma`；因根目录已有 `DESIGN.md`，模板保存至 `~/figma/DESIGN.md`，已 `cp` 到项目根目录覆盖。
- 按 Figma 策展规范重做：`css/style.css`（界面纯 `#000`/`#fff`、负字距、Inter + IBM Plex Mono、`kern`、50px 胶囊按钮、2px 虚线 focus；Hero 与展示带使用多色渐变）；`index.html` 改为 Hero 渐变 + 白底卡片栅格 + 渐变研究带 + 深色页脚。
- 子页更新字体引用，顶栏统一为白底（移除 SpaceX 黑底顶栏类名）。

### 说明

- `figmaSans` / `figmaMono` 以 Google Fonts **Inter**、**IBM Plex Mono** 近似实现；字重 330/350/540 等依赖可变字体插值。

## 2026-04-10（Ollama + 文章双栏）

### 成功

- 运行 `npx getdesign@latest add ollama`；模板存于 `~/ollama/DESIGN.md`，已复制为项目根目录 `DESIGN.md`。
- 全站样式改为 Ollama 策展风：纯白底、全灰阶、零阴影与渐变、12px 容器圆角、胶囊按钮与导航、系统 UI 字体栈；焦点环使用半透明蓝（与 DESIGN 一致）。
- 首页文案调整为「个人最新动态与工作」定位；移除 Figma 式彩色 Hero。
- 文章详情页：`projects/research-mribag.html`、`projects/tsla-options-war-room.html` 采用 **左侧 `doc-toc` 目录 + 右侧 `doc-content` 正文**，章节带 `id` 锚点；新增 `js/article-toc.js` 按滚动高亮当前目录项。
- 子页去掉外链 Webfont，依赖系统字体。

### 说明

- Ollama 主题为第三方策展参考，非官方；网站用途为个人更新展示，长文用侧栏目录提升可读性。

## 2026-04-10（结构重构：去导航栏 · feed 式首页）

### 成功

- 彻底重构站点结构：**移除导航栏、卡片网格、Hero 区、分区标题**。
- **首页**（`index.html`）：仅剩名字 + 一句话 + 时间线式 feed 列表（日期 · 标题 · 简述 · 标签），条目即链接。
- **详情页**：纯双栏（左目录 + 右正文），顶部只有 `← Zhiyuan` 返回链接，不再有任何顶栏或导航结构。
- **CSS**：去掉全部 header/nav/card/hero/footer 类；新增 `.page-home`、`.identity`、`.feed`、`.feed-item`、`.page-doc`、`.doc-layout` 等；页面宽度收窄（首页 640px、详情 1060px）。
- **JS**：`main.js` 保留但不再被首页使用（首页无可切换导航）；`article-toc.js` 照常工作。

### 说明

- 首页不再需要 JavaScript（无汉堡菜单），但子页仍引用 `main.js`（预留未来扩展）。

## 2026-04-10（Cal.com 策展风 · 完整展示站）

### 成功

- 运行 `npx getdesign@latest add cal`；模板存于 `~/cal/DESIGN.md`，已复制为项目根目录 `DESIGN.md`。
- 全站按 Cal.com 策展规范重设计：
  - 引入 Google Fonts **Cal Sans**（展示标题）、**Inter**（正文）、**Roboto Mono**（代码/标签）。
  - 色板回归灰阶单色：Charcoal `#242424` 标题、Mid Gray `#898989` 正文、Link Blue `#0099ff` 链接。
  - 多层阴影卡片系统：ring-border + 漫射 + 接触阴影三合一，hover 时阴影加深。
  - 通栏呈现而非极简 feed——恢复 Hero 区（Cal Sans 大标题）、卡片网格（Projects / Research 各一组）。
  - 顶栏回归：白色磨砂背景 `backdrop-filter` + 固定定位，Cal Sans 品牌名 + Inter 导航 + 深色 CTA 按钮。
  - 页脚浅灰色背景，版权 + 链接。
- **首页**（`index.html`）：Hero → 项目卡片区 → 研究卡片区 → 页脚。
- **详情页**（`projects/research-mribag.html`、`projects/tsla-options-war-room.html`）：保留左侧 TOC + 右侧正文双栏；TOC 容器改用多层阴影卡片；`scroll-margin-top` 调整为顶栏高度兼容；顶部返回链接 + 统一顶栏。
- CSS 阴影替换所有 `border`（用 ring-shadow 代替，符合 Cal.com 哲学）；宽松节间距 80–96px。
- `js/article-toc.js` 保留不变（IntersectionObserver 高亮逻辑通用）。

### 说明

- Cal Sans 由 Google Fonts CDN 提供，离线环境回落到 Inter → system-ui。
- 相比之前的极简 feed 式首页，Cal.com 主题下恢复了完整展示结构（导航、Hero、卡片、页脚），更适合"个人展示"定位。

## 2026-04-10（TSLA 项目页充实文档）

### 成功

- `projects/tsla-options-war-room.html`：按 SKILL.md + README 级别重写为长文档，含 13 个目录锚点（概述、元数据、动机与新颖性、设计取向、与 Bot 对比、架构 ASCII、Agent 表、交互与三种深度、多空与多周期与 QQQ、数据路径与量价规则、仓位止盈止损、生命周期与绩效、安装使用、免责声明）。
- 补充英文 `description` 原文触发说明；架构、双目录职责、目录树、交易参数表、完整报告骨架、流式规则、Simulation 与心态提醒等均已写入正文。
- `css/style.css`：为长文增加 `.article-body h3`、表格、`pre.doc-pre` 样式，便于项目文档页排版。

### 失败

- 无。

## 2026-04-10（VPCT iOS 项目文档页）

### 成功

- 新增 `projects/vpct-vpip-controller.html`：VPCT（VPIP Controller）MVP 架构与方案长文档，含 16 节目录（概述、4 Tab、技术栈、Guest/登录、SwiftData 模型表、手牌录入、数据管理、Session UI 状态、玩家类型、Tilt 三层与五检测器、冷静期状态机与 isStillDeviating、设置开关表、本地化、Stats 访问控制、实现清单、强弱牌参考、文档版本）。
- `index.html`：用 VPCT 项目卡片替换原「下一个项目」占位，链接至上述文档页。

### 失败

- 无。

## 2026-04-10（KLineLens 开源项目文档页）

### 成功

- 新增 `projects/klinelens.html`：KLineLens 长文档（概述、开源定位「自选 API + 自写策略」、Why、功能表、0DTE 状态机、Provider 表、架构 ASCII、数据流与缓存、里程碑与 MVP 验收、策略扩展、Docker/本地快速开始、文档索引、贡献要点、Roadmap、MIT 与免责声明），侧栏目录与 `article-toc.js` 联动。
- 元信息注明项目自 2026-01-13 起做，并链至 `https://github.com/songzhiyuan98/KLineLens`。
- `index.html`：第三张项目卡由占位改为 KLineLens，链至 `projects/klinelens.html`；子页页脚增加 GitHub 外链。

### 失败

- 无。

## 2026-04-10（Novel Studio 项目文档页）

### 成功

- 新增 `projects/novel-studio.html`：Novel Studio 长文档，含目录 14 节（概述、功能进度、RAG 两层计划、架构四平面、编排与 Agent、工作流、Packet 与 token 预算、QA、安全规则、技术栈、仓库结构、测试与成本、快速开始、仓库 docs 索引、声明）；融合 README 与 Downloads 中 workflow / RAG / prompts-packets / QA / agents / architecture 摘要。
- `index.html`：项目区新增第四张卡片 Novel Studio，链至 `projects/novel-studio.html`。

### 失败

- 无。

## 2026-04-10（Novel Studio · RAG 混合策略扩写）

### 成功

- `projects/novel-studio.html`「复杂 RAG」整节重写加长：`h2` 设 `id="rag"` 便于目录高亮；新增子节——混合策略总览（为何向量/关键词/结构化单独都不够）、四层流水线表（L1–L4）、RRF/加权融合思路、**Mode S 静态 packet + Mode R 检索增强** 双轨、按 Planner/Writer/QA/Summarizer 差异化检索、**QA 触发二次检索**、双索引与缓存与正典化后作业、评测对比指标；重申检索结果必经 Packet Compiler。
- `css/style.css`：为 `.doc-section h2[id]` 与 `.article-body h3[id]` 增加 `scroll-margin-top`，长文内锚点不被顶栏遮挡。

### 失败

- 无。

## 2026-04-10（Novel Studio · RAG 对齐核心规范「两层计划」）

### 成功

- `projects/novel-studio.html`：RAG 节改为与 `docs/superpowers/specs/2026-03-24-core-architecture-design.md` 一致的**两层口径**：L0 结构化正典召回（场景卡元数据键、无嵌入）、P0–P4、长篇示例（约 12K tokens 覆盖 350 章历史）、L1 语义检索目标与启动门禁、L0→L1 合并去重流水线、各 Worker 检索用例表、MVP 不含 L1 边界；并说明 `rag-optimization-strategy.md` 等为工程草案、产品口径以 L0/L1 为准。
- 侧栏「复杂 RAG」更名为「RAG 两层计划」；架构脚注与 Packet 节首段增加与 `#rag` 的交叉引用。
- `index.html`：Novel Studio 卡片改为 L0/L1 两层计划简述。

### 失败

- 无。

## 2026-04-10（Novel Studio · 仓库链接与起始日期）

### 成功

- `projects/novel-studio.html`：元信息注明项目自 **2026-01-30** 起做；仓库展示为完整 URL `https://github.com/songzhiyuan98/Novel-Studio`，并注明与 GitHub 仓库副标题 *A Multi-Agent System for Long-Form Fiction Generation* 一致。
- `index.html`：Novel Studio 卡片 kicker 增加「自 2026-01-30」。

### 失败

- 无。

## 2026-04-10（Novel Studio · 检索关键词 meta + 文案）

### 成功

- `projects/novel-studio.html`：`<head>` 增加 `meta keywords` / `description`（含 multi-agent、hybrid RAG、advanced RAG、L0/L1 等）；概述区增加可见「关键词」一行，首段点明 multi-agent 与混合式高级 RAG 演进。
- `index.html`：Novel Studio 卡片 kicker 与描述纳入 Multi-agent、Hybrid/Advanced RAG 等用语。

### 失败

- 无。

## 2026-04-10（Novel Studio · 仅 hybrid RAG + 日期到月）

### 成功

- 全文对外统一为 **hybrid RAG**（去掉 advanced RAG、Hybrid/Advanced 并列与「混合式高级」等）；项目时间改为 **2026-01**（写到月份）：`projects/novel-studio.html` 元信息、`meta` 字段、概述关键词与首段；`index.html` 卡片 kicker 与描述。

### 失败

- 无。

## 2026-04-10（TalentSync 项目页）

### 成功

- 新增 `projects/talentsync.html`：TalentSync 文档页，含概述、演示与仓库链接（`https://github.com/songzhiyuan98/TalentSync`、`https://talentsync-green.vercel.app`、邮箱）、核心功能、**Stripe Free/Premium/Enterprise** 表、前后端技术栈与部署、目录结构、快速开始、API 摘要、安全、贡献与 MIT。
- 元信息：**2025-03** 起做；**最近维护距今约三个月**（相对说明，以 GitHub 提交为准）。
- `index.html`：项目区新增 TalentSync 卡片。

### 失败

- 无。

## 2026-04-10（TalentSync · 标题仅英文名）

### 成功

- `projects/talentsync.html`：页面 `<title>`、`<h1>`、`meta description` 去掉「人才同步」括号，仅保留 **TalentSync**；浏览器标签标题统一为 `TalentSync · 项目文档`。

### 失败

- 无。

## 2026-04-10（首页项目卡片去掉粗体）

### 成功

- `index.html`：Novel Studio、TalentSync 卡片描述中的 `<strong>` 已全部移除，与其他项目卡片一致，主次交给标题与 kicker。

### 失败

- 无。

## 2026-04-10（TalentSync · 描述扩充与链接修复）

### 成功

- `projects/talentsync.html`：概述改为三段，突出多视角匹配、求职信/PDF、JD 与状态流、Stripe/OAuth、部署与演示；明确引导至 GitHub README 与 `PROJECT_DOCUMENTATION.md`；`meta description` 同步；修复多处错误的 `</a>` 断行闭合。
- `index.html`：TalentSync 卡片文案加长，并写明「完整能力、API 与配置以 GitHub README 为准」。

### 失败

- 无。

## 2026-04-10（首页 TalentSync 卡片精简）

### 成功

- `index.html`：TalentSync 卡片描述改为一小段，去掉堆叠的功能枚举；保留「匹配 + 求职信/投递 + Stripe/Google」与「项目页 / GitHub」引导。
- `index.html`：页脚外链 `<a>` 合并为合法闭合标签（修复误拆的 `</a>`）。

### 失败

- 无。

## 2026-04-10（首页 TalentSync 卡片加力度）

### 成功

- `index.html`：TalentSync 卡片改为三句左右的中等篇幅：强调多视角报告线、可落地策略、求职信/PDF/JD 与状态流、Stripe/OAuth/多模型、Vercel+Railway 与可跑通演示，并指向 GitHub 文档。

### 失败

- 无。

## 2026-04-10（TalentSync 恢复初版表述）

### 成功

- `projects/talentsync.html`：`<title>`、`meta description`、`<h1>` 恢复带「人才同步」副标题；概述改回两段式简短介绍（技术栈 + 功能要点 + README 指引），去掉后续迭代中的长段扩写与 GitHub 文档强引导段落。
- `index.html`：TalentSync 首页卡片恢复为初版短描述，并恢复描述中的 `<strong>全栈 AI 求职助手</strong>`。

### 失败

- 无。

## 2026-04-10（TalentSync 去掉「人才同步」与首页粗体）

### 成功

- `projects/talentsync.html`：`<title>`、`<h1>`、`meta description` 去掉「人才同步」，仅保留 **TalentSync**；注释同步说明。
- `index.html`：TalentSync 卡片描述去掉 `<strong>`，与其它项目卡片一致。

### 失败

- 无。

## 2026-04-10（首页 TalentSync 卡片 kicker）

### 成功

- `index.html`：TalentSync 卡片 `card__kicker` 由 `React · Stripe · 2025-03` 改为 **Fullstack · AI application · 2025-03**，突出全栈与 AI 应用定位。

### 失败

- 无。

## 2026-04-10（AI 学习助手 Study Assistant 项目页）

### 成功

- 新增 `projects/study-assistant.html`：基于你提供的 README 素材整理为站内文档（概述、仓库、核心特性、快速开始、使用流程、功能进度表、技术栈、目录结构、常用命令、贡献与许可）；修正机翻用词（TypeScript、Next.js、PostgreSQL、Tailwind、Prisma、Redis、PM2、Fork 等），去掉正文标题中的装饰性 emoji。
- 仓库链接暂写为 `https://github.com/songzhiyuan98/study-assistant`（与其它项目同一 GitHub 账号约定）；若实际仓库名或组织不同，只需改该页与首页卡片链接。
- `index.html`：项目区新增 **AI 学习助手** 卡片（kicker：`Monorepo · Next.js · pgvector · 2025-09`），链至上述文档页。

### 失败

- 未验证远端仓库是否存在（本地曾见 `study-assistant` 路径；若未公开或改名，链接需手动更正）。

## 2026-04-10（AI 学习助手 · 系统架构与路线图）

### 成功

- `projects/study-assistant.html`：扩充「系统架构设计」— 架构概述与数据流示意、上传/生成两条流水线、数据模型（实体树 + 字段表）、前后端与 Worker 目录边界、向量检索（IVFFlat / 余弦 / Top-K）、RBAC 与审计、BullMQ 任务类型与优先级、可观测性 KPI 与 Prometheus/Grafana/ELK/PagerDuty 规划、REST 与关键 API 摘要。
- 合并「功能进度」与 **2025-09-08** 进度快照（已完成项、进行中、完成度百分比）及原简表。
- 新增「开发路线图」三节（第 1–3 阶段按周任务）与「下一步行动」清单；概述段注明与 `docs/ARCHITECTURE.md` / `ROADMAP.md` 对齐。
- `meta description` 更新以反映架构与路线图内容；侧栏目录增加对应锚点。

### 失败

- 无。

## 2026-04-10（个人网站模板 Personal Website Template）

### 成功

- 新增 `projects/personal-website-template.html`：概述 [songzhiyuan98/personal-website-template](https://github.com/songzhiyuan98/personal-website-template) 双模板（求职者 / 研究者）、特性列表、技术栈、README 口径的采用数据、快速开始（正确克隆地址 `personal-website-template`；注明旧名 `personal-website-temp` 已废弃）、MIT；链至展示站 `https://www.zhiyuansong.dev`（标注以仓库为准）。
- `index.html`：在 Novel Studio 与 TalentSync 之间新增 **个人网站模板** 卡片（kicker：`Next.js · Portfolio · MIT · 2025-01`）。

### 失败

- 无。

## 2026-04-10（首页项目区：年份分组 + CTA 统一）

### 成功

- `index.html`：项目卡片按 **2026 / 2025** 分块展示（`section__year-group` + `section__year-label`，锚点 `work-2026` / `work-2025`）；组内自新到旧——2026：TSLA（04）→ VPCT（进行中，kicker 补 `2026`）→ KLineLens / Novel（01）；2025：学习助手（09）→ TalentSync（03）→ 个人网站模板（01）。
- 项目区卡片 CTA 统一为 **「阅读项目文档 →」**（含原 Skill、原 MVP 方案页）；**研究区**论文卡片仍用 **「阅读摘要 →」**（体裁不同）。HTML 注释中写明约定。
- `css/style.css`：新增 `.section__year-group`、`.section__year-label` 样式。

### 失败

- 无。

## 2026-04-10（首页项目/研究卡片描述统一优化）

### 成功

- `index.html`：七张项目卡与一张研究卡描述改为统一节奏——**前句定位与场景，后句技术栈或交付形态**；去掉与 kicker 重复的堆叠枚举、营销数字与「见项目页」类空话；区块 HTML 注释同步说明写法约定。

### 失败

- 无。

## 2026-04-10（动漫中心 AnimeHub · 2024 全栈项目）

### 成功

- 新增 `projects/animehub.html`：概述首个全栈动漫社区（Jikan、缓存、评分评论、帖子与 Socket、i18n）、前后端技术栈、实现要点摘要、快速开始与目录结构；MIT 与联系邮箱；部署侧提及 Heroku / Netlify 实验。
- 仓库链接已确认为 `https://github.com/songzhiyuan98/Animehub`（仓库名大小写以 GitHub 为准）；子目录仍为 `animehub-backend` / `animehub-frontend`。
- `index.html`：项目区新增 **2024** 年份分组（`#work-2024`）与 **动漫中心（AnimeHub）** 卡片，描述与全站卡片句式对齐。

### 失败

- 无（后续已确认仓库 slug 为 Animehub，见下条）。

## 2026-04-10（AnimeHub 仓库链接更正）

### 成功

- `projects/animehub.html`：GitHub 统一为 [songzhiyuan98/Animehub](https://github.com/songzhiyuan98/Animehub)；克隆示例改为 `Animehub` 目录名；删除「中文仓库名另替」的说明。

### 失败

- 无。

## 2026-04-10（MRIBAG 研究页英文扩写）

### 成功

- `projects/research-mribag.html`：全文改为 **英文**（`lang="en"`），按 Nature Medicine 正文扩写——Key numbers 增加蛋白/代谢关联计数；新增 Background、Methods 五块摘要、Results（模型 / ProWAS–MetWAS / 遗传与可成药靶点 / 临床与 A4）、Discussion 与局限；保留官方 Abstract；Data &amp; code 含 MEDICINE、Synapse、Zenodo、ProWAS/MetWAS/GDI 链接；Citation 与 `meta description` 更新。HTML 注释保留中文以符合仓库约定。
- `index.html`：研究卡片标题与描述改为英文，CTA 为 **Read summary →**。

### 失败

- 无。

## 2026-04-10（首页顺序：求职优先 + 顶栏对齐）

### 成功

- `index.html`：区块顺序改为 **项目 → 研究 → 随笔**（招聘方先看到交付物，研究作背书，随笔承载观点）；顶栏 **项目 / 研究 / 随笔** 与滚动顺序一致；`hero__sub` 改为兼顾求职与个人品牌的一句说明；项目区 HTML 注释写明三者分工。
- 各 `projects/*.html` 顶栏链接顺序与首页一致（项目 → 研究 → 随笔）。

### 失败

- 无。

## 2026-04-10（首页：随笔板块 + 研究置于项目之上）

### 成功

- `index.html`：在「项目」之上新增 **随笔**（`#writing`，kicker Notes），导语 + 卡片「Agent skills: constraints over scripted workflows」链至 `projects/writing-agent-skill-constraints.html`，CTA **阅读全文 →**；占位 **Next note**。区块顺序为 **随笔 → 研究 → 项目**；顶栏导航同步为 **随笔 / 研究 / 项目**。
- 新增 `projects/writing-agent-skill-constraints.html`：中文随笔正文（缘起、规定路径问题、约束与边界、Skill 实践、收束），左侧目录 + `article-toc.js`；顶栏与返回链指向 `#writing`。
- 各 `projects/*.html` 顶栏在「项目」「研究」前增加 **随笔** 链至 `#writing`。

### 失败

- 无。

## 2026-04-10（首页卡片标题统一英文）

### 成功

- `index.html`：项目区原中文 `h3`（AI 学习助手、个人网站模板、动漫中心）与占位卡、研究区两条论文卡片的 `h3` 均改为**英文标题**；研究/占位 kicker 与正文描述仍为中文；项目区注释补充「卡片标题（h3）统一为英文」。

### 失败

- 无。

## 2026-04-10（研究区与两篇详情页改回中文）

### 成功

- `index.html`：研究区两条卡片标题、描述、kicker 与 CTA **阅读摘要 →** 均改为中文；占位卡中文；项目区注释中研究区 CTA 改回「阅读摘要」。
- `projects/research-sleep-charts.html`、`projects/research-mribag.html`：`lang="zh-Hans"`，标题与 **目录、正文、摘要意译、方法/结果/讨论、数据与代码、页脚** 全中文；论文英文题目保留在 `h1`；作者行中英混排（中文姓名 + 原文作者名）；外链与引用块保留。
- HTML 注释保留中文以符合仓库约定。

### 失败

- 无。

## 2026-04-10（首页研究区卡片：改回英文）

### 成功

- `index.html`：研究区两条论文卡片描述与占位卡、CTA **Read summary →** 均改回英文；项目区注释内研究区 CTA 说明同步为 Read summary。

### 失败

- 无。

## 2026-04-10（首页研究区卡片描述精简）

### 成功

- `index.html`：研究区两条论文卡片改为**各一句中文**描述，与 kicker 分工（期刊/状态/时间 vs 内容要点）；占位卡一句化；CTA 与项目区统一为 **阅读摘要 →**；上方增加 HTML 注释约定。

### 失败

- 无。

## 2026-04-10（MRIBAG：Nature Medicine 正式文章链接）

### 成功

- `projects/research-mribag.html`：页眉增加 **Open-access article** 指向 https://www.nature.com/articles/s41591-025-03999-8；Citation 与页脚原 `nature.com/nm/` 期刊首页改为该文 URL（页脚标签改为 **Nature Medicine**）；`meta description` 含同一链接。
- `index.html`：MRIBAG 研究卡片一句提示正式文章路径（无嵌套 `<a>`，全文链在研究笔记页眉）。

### 失败

- 无。

## 2026-04-10（Sleep chart 研究页对齐 MRIBAG 篇幅）

### 成功

- `projects/research-sleep-charts.html`：删去长文式多节正文，改为与 `research-mribag.html` 同构的**精简研究笔记**——TOC 为 Overview、Key numbers（两行 stat-strip）、Abstract、Background、Methods 摘要、Results 三节（Sleep–BAG / Genetics–survival / LLD）、Discussion &amp; limitations、Data &amp; code、Citation；页眉保留作者一行、PMC 全文 + DOI + PMID；页脚 Home / Sleep Chart / Preprint (PMC) / GitHub；保留中文 HTML 注释。
- `index.html`：Sleep chart 卡片描述缩短并与 MRIBAG 卡片语气一致，CTA 改为 **Read summary →**。
- 详细图表说明、Method 1–9 全文、Extended Data 逐条等改由 **PMC 全文** 与 **Sleep Chart 门户**承担，个人页只做与 MRIBAG 页相当的摘要。

### 失败

- 无。

## 2026-04-10（Sleep chart：预印本全文链到 PMC）

### 成功

- `projects/research-sleep-charts.html`：页眉将 **https://pmc.ncbi.nlm.nih.gov/articles/PMC12363694/** 标为 **Preprint (full text)**；medRxiv DOI 与 PubMed 作为并列入口；引用块与页脚 **Preprint (PMC)** 指向该 URL，并单独保留 **DOI** 链；`meta description` 同步。
- `index.html`：研究卡片文案标明预印本全文在 **PMC12363694**（链接置于研究笔记页眉，避免整块卡片 `<a>` 内嵌套外链）。

### 失败

- 无。

## 2026-04-10（Sleep chart 研究页：按 PMC 正文再扩写）

### 成功

- `projects/research-sleep-charts.html`：在既有摘要/作者/标识基础上，按预印本正文增补 **Introduction**、**Aims & design**（四点研究问题）、**Figures 1–4** 说明、**Results**（U 形 BAG 与 EDF/最优睡眠、GWAS/MAGMA/LDSC/PRS、遗传相关与 Cox/TriNetX、LLD 中介与 MR）、**Discussion** 压缩段、**Extended Data** 五图要点、**Methods 1–9** 分节叙述、**Limitations** 整段、**Data & code**（数据外链 + 与论文一致的软件版本列表）、**Acknowledgments**（基金号 UKBB 申请号 + MULTI 代表一句）；TOC 与 `article-toc.js` 锚点同步；`meta description` 更新为长文笔记定位。
- HTML 内保留中文注释以符合仓库约定。

### 失败

- 无。

## 2026-04-10（Sleep chart 研究页按全文补全）

### 成功

- `projects/research-sleep-charts.html`：由占位摘要扩写为完整英文研究笔记——更新 TOC（Overview、Authors、Abstract、Key findings、Sleep Chart 子页、Methods 压缩、Limitations、Data & code、Citation）；嵌入官方 **Abstract** 全文；作者行按预印本顺序（含 **Zhiyuan Song**、通讯 **Junhao Wen**）；DOI / PMID / PMC 外链；Key findings 分四块概括 U 形 BAG、多组学扩展、遗传与生存、LLD 中介；门户链 `sleep_gc.html`、`sleep_cox.html`；Synapse / MEDICINE / 方法栈与资助一句；页脚增加 Sleep Chart 链；`meta description` 修正为 PMID + PMC 编号表述。
- `index.html`：Sleep chart 卡片描述与 CTA 与扩写后的研究页一致（**Read note →**）。
- HTML 内保留中文注释以符合仓库约定。

### 失败

- 无。

## 2026-04-10（Sleep chart 状态：Accepted by Nature）

### 成功

- `projects/research-sleep-charts.html`：`meta description`、页眉状态、Publication status 与 provisional 引用统一为 **Accepted by Nature**（保留 forthcoming 说明）。
- `index.html`：研究卡片 kicker 与正文描述同步为上述用语。

### 失败

- 无。

## 2026-04-10（研究：Sleep chart 预印本 / Nature 接收）

### 成功

- 新增 `projects/research-sleep-charts.html`：标题与 PDF 元数据一致（*Sleep chart of biological aging clocks across organs and omics*）；标注预印本 DOI [10.1101/2025.08.08.25333313](https://doi.org/10.1101/2025.08.08.25333313)；**Publication status** 与后续迭代用语见上条「Accepted by Nature」；英文摘要式说明与 provisional 引用块。
- `index.html`：研究区新增卡片（置于 MRIBAG 之前）；kicker 现为 **Accepted by Nature · Preprint 2025-08**（与正文状态用语一致）。

### 失败

- 未能从 PDF 抽取完整作者列表与摘要正文（环境无 `pdftotext`）；成刊后需据 Nature 网页补全作者顺序与最终 DOI。

## 2026-04-10（全站 Footer 统一）

### 成功

- `css/style.css`：页脚统一顶部分隔线、内边距与字重；版权区与链接区使用等宽小字号；链接为大写等宽标签、hover 下划线；`@media (max-width: 560px)` 时主行纵向堆叠。
- 全站 `footer`：`role="contentinfo"`；链接区改为 `<nav class="site-footer__links" aria-label="…">`；版权句统一为 **© 2026 Zhiyuan Song. All rights reserved.**（含 `research-mribag.html` 原缺后半句）。
- `index.html`：GitHub 指向 `https://github.com/songzhiyuan98`；Scholar 标签缩短为 **Scholar** 以适配页脚样式。
- `projects/vpct-vpip-controller.html`、`projects/tsla-options-war-room.html`：补充 GitHub（账号或 Skill 仓库）；其余项目子页在原有仓库/Demo 链上套统一结构。

### 失败

- 无。

## 2026-04-10（子页接入中英文切换）

### 成功

- `js/site-i18n.js`：新增英文键 `footerPreprintPmc`（研究 Sleep 页脚「预印本 (PMC)」）。
- `projects/*.html`（共 11 个）：顶栏「项目 / 研究 / 随笔」加 `data-i18n`；增加 `#site-lang-toggle` 语言按钮；「← 返回首页」用 `navBack`，随笔文 `writing-agent-skill-constraints.html` 用 `navBackWriting`；页脚「首页」用 `footerHome`；随笔页脚「随笔」用 `navWriting`；`research-sleep-charts.html` 页脚 PMC 链用 `footerPreprintPmc`；在 `article-toc.js` 之前引入 `../js/site-i18n.js`。与首页共用 `localStorage.siteLang`，子页与首页语言一致。

### 失败

- 无。

## 2026-04-10（首页随笔区：去掉导言）

### 成功

- `index.html`：「随笔」区块下删除导语段落；占位卡文案由「后续工程笔记…」改为 **后续文章将接在此处。**，避免限定为工程向。

### 失败

- 无。

## 2026-04-11（新项目草案：Investment Skill 架构说明 · 中英页面）

### 成功

- 新增 **`projects/investment-skill-architecture.html`**（中文）：完整对应规范结构 1–16 节；保留 **§7 各 Agent 具体设计**（7.1–7.8）与 **§2 协同工作总纲 / §5 编排协同分级** 等板块；顶栏 **English** → `/en/projects/investment-skill-architecture.html`。
- 新增 **`en/projects/investment-skill-architecture.html`**（英文）：与中文镜像同锚点 `id`，便于对照；正文为规范英文整理稿。
- **`index.html`**、**`en/index.html`**：2026 区卡片列表**最前**插入 **Investment Skill Architecture**（kicker 标明设想 / Draft），链至上述文档。

### 失败

- 无。

## 2026-04-11（hero：近期工作 / 进度 / 研究 / 笔记感悟）

### 成功

- `index.html`：`hero__sub` 改为 **收录近期工作、进度、研究与笔记里的感悟等。**
- `en/index.html`：对应英文 **Recent work, progress, research, and reflections from notes—collected here.**

### 失败

- 无。

## 2026-04-11（Vercel：根路径静默英文 + hero 文案）

### 成功

- **`vercel.json`**：由 **`redirects`（302 跳 `/en/`）** 改为 **`rewrites`**：`/` **内部映射**到 **`/en/index.html`**，地址栏仍为 **`/`**，避免「闪一下再跳」的体验；中文首页仍为 **`/index.html`**。
- **`en/index.html`**：内链改为 **`/en/...`**、**`/css/style.css`** 等**站点根绝对路径**，保证在「根路径显示英文首页」时卡片与导航仍指向英文子页；**「中文」** → **`/index.html`**。
- **`index.html`**（中文）：`hero__sub` 改为 **「项目、研究与随笔，统一放在这个站点。」**；**English** 链为 **`/en/`**。
- **`projects/*.html`**：**English** 链统一为 **`/en/projects/<同名>.html`**（绝对路径）。

### 失败

- 无。

## 2026-04-11（中英双站：/en 镜像 + 语言切换）

### 成功

- 新增 **`en/index.html`**：英文首页（导航 Projects / Research / Notes、卡片与 CTA 英文、页脚 Scholar / Email）；顶栏与页脚提供 **`中文`** 链回根目录 `index.html`。
- 新增 **`en/projects/*.html`**（11 个）：与中文 `projects/` 一一对应；资源路径为 **`../../css/style.css`**、**`../../js/article-toc.js`**；站内链到 **`../index.html`**（英文首页）；顶栏 **`中文`** → **`../../projects/<同名>.html`**。
- **`projects/*.html`**：顶栏在「随笔」后增加 **`English`** → **`../en/projects/<同名>.html`**。
- **`index.html`**：顶栏与页脚增加 **`English`** → **`en/index.html`**。
- **`css/style.css`**：新增 **`.lang-switch`** 样式（中英互链轻量链接）。
- 英文正文：大篇幅项目/研究页（含 `vpct-vpip-controller.html`、`novel-studio.html`、`study-assistant.html`、`tsla-options-war-room.html`、`talentsync.html`、`klinelens.html`、`research-sleep-charts.html`、`research-mribag.html`）由助手批量英文化；**随笔 Skill 文**、**个人网站模板**、**AnimeHub** 三页由当前会话补写完整英文稿。
- `en/projects/vpct-vpip-controller.html`：应用内语言标签表行「中文」改为 **Chinese**，避免英文页正文残留汉字。
- `en/projects/tsla-options-war-room.html`：Skill 描述改为英文概括触发场景，避免整段中文触发词贴在页内；细节仍以仓库 `SKILL.md` 为准。

### 失败

- 无。

## 2026-04-10（首页：Scholar 与个人邮箱）

### 成功

- `index.html`：页脚 **Scholar** 指向个人主页 `https://scholar.google.com/citations?user=wh3s5WkAAAAJ&hl=zh-CN`；顶栏「联系我」与页脚 **Email** 统一为 `mailto:songzhiyuan98@gmail.com`。

### 失败

- 无。

## 2026-04-10（首页锚点与 sticky 顶栏）

### 成功

- `css/style.css`：为带 `id` 的 `.section` 与 `h3.section__year-label` 增加 `scroll-margin-top: 88px`，使 `#work` / `#research` / `#writing`（及 `#work-2026` 等）滚动定位时不被顶栏挡住。顶栏链接与 `index.html` 内区块 `id` 本就一致，未改 HTML。

### 失败

- 无。

## 2026-04-10（研究页作者行：宋致远 → Zhiyuan Song）

### 成功

- `projects/research-sleep-charts.html`、`projects/research-mribag.html`：英文作者列表中与拉丁姓名体例统一，将 **宋致远** 改为 **Zhiyuan Song**；Sleep 页元信息中误写的 `The MULTI study#` 改为 `The MULTI study，`。

### 失败

- 无。

## 2026-04-10（首页 hero 副标题恢复中性表述）

### 成功

- `index.html`：`hero__sub` 由「个人网站：…兼顾求职与个人品牌」改回偏归档向的一句：**收录项目实践、研究笔记与工程随笔。**

### 失败

- 无。

## 2026-04-10（移除 i18n，恢复纯中文）

### 成功

- 删除 `js/site-i18n.js`；`index.html` 与 `projects/*.html` 去掉所有 `data-i18n`、`#site-lang-toggle` 及脚本引用；首页注释中删除语言切换说明。
- `css/style.css`：移除 `.lang-toggle` 样式块。
- 页面仍以 HTML 内中文为唯一展示文案，`lang="zh-Hans"` 不变。

### 失败

- 无。
